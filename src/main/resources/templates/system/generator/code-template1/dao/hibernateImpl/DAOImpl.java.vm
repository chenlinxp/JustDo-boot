package ${package}.${classname}.dao.hibernateImpl;
import org.springframework.stereotype.Repository;
import com.sxsihe.oxhide.persistence.BaseDAOImpl;
import ${package}.${classname}.dao.${className}DAO;
import ${package}.${classname}.domain.${className};

/**
 * ${comments}
 * Description：
 * @author: ${author}
 * @email: ${email}
 * @date: ${datetime}
 * Modification History:  
 * Modified by :  
 */
@Repository("${classname}DAO")
public class ${className}DAOImpl extends BaseDAOImpl implements ${className}DAO {
	@Override
	public Class<?> getEntityClass() {
		// TODO Auto-generated method stub
		return Answer.class;
	}
}