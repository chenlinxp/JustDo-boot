package ${package}.${classname}.action;

import java.io.Serializable;
import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.ModelAndView;

import com.sxsihe.oxhide.controller.BaseSaveAction;
import com.sxsihe.oxhide.exception.BaseException;
import com.sxsihe.oxhide.service.BaseServiceIface;

import ${package}.${classname}.domain.${className};
import ${package}.${classname}.service.${className}Service;

/**
 * ${comments}
 * Description：
 * @author: ${author}
 * @email: ${email}
 * @date: ${datetime}
 * Modification History:  
 * Modified by :  
 */
 
@Controller
@RequestMapping("/${classname}.SaveAction")
public class ${className}SaveAction extends BaseSaveAction {
	private ${className}Service ${classname}Service;
	@Override
	@Resource(name = "${classname}Service")
	public void setService(BaseServiceIface service) {
		// TODO Auto-generated method stub
		${classname}Service=(${className}Service) service;
		super.setService(service);
	}
	@RequestMapping(params = "action=add")
	public ModelAndView add(${className} ${classname},HttpServletRequest request, HttpServletResponse response) throws BaseException{
		Serializable id=getService().save(${classname});
		return super.add(id, request, response);
	}
	@RequestMapping(params = "action=update")
	public ModelAndView update(${className} ${classname},HttpServletRequest request, HttpServletResponse response) throws BaseException{
		Serializable id=getService().update(${classname});
		return super.add(id, request, response);
	}
}